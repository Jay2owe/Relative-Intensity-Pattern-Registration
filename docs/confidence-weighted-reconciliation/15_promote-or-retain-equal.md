# Stage 15 - Promote the passing policy or retain equal

## Objective

Apply the Stage 08 product rules to the new frozen evidence.

## Outcomes

- `PROMOTE_GLOBAL`
- `PROMOTE_SCOPED`
- `PROMOTE_SELECTOR`
- `NO_PROMOTION`

Promotion adds explicit Java/Python parameters and provenance, keeps equal replayable and preserves the separate rigid-estimator fallback. No main-dialog tuning control is added unless the benchmark separately demonstrates useful manual choice. Full Java, Python, macro and API tests must pass before a default changes.
