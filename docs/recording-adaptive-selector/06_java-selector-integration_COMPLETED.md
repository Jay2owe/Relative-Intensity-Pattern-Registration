# Stage 06 completed

The generated selector is integrated into the Java registration path, preserves rigid settings on fallback, exposes complete provenance and skips unused evidence work. Selector and runtime-constant regression tests pass.
