# These nested public-release .gitignore files intentionally ignore local agent
# context filenames; the pattern itself contains no private path or credential.
^publication/Log-Ratio Registration-public-release/\.gitignore$
^publication/Log-Ratio Registration-public-verify/\.gitignore$
